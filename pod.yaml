apiVersion: v1
kind: Pod
metadata: 
  name: loopr-pod1
  namespace: argocd
  labels:
    app: web
spec:
   containers: 
     name: nginnnx-v1
     image: nginx:latest
     ports:
      containerPort: 80
